---
title: 迭代器
comments: true
copyright: false
cover: 'https://tva1.sinaimg.cn/large/87c01ec7gy1fsnqqi27uej21kw0w04cl.jpg'
date: 2020-08-01 14:38:40
tags:
categories: python基本语法
photo:
top:
toc: true
---



## 1.什么是迭代器

   迭代:一个重复的过程 ，每次重复都基于上一次的结果而继续。单纯的重复并不是迭代。
   迭代器: 一种不依赖于索引的用来迭代取值的工具

## 2.如何使用迭代器？

   可迭代对象:所有内置有__iter__方法的对象，都是可迭代对象
   字符串，字典，元组，集合，列表，打开的文件都是可迭代对象

## 3.objet.__iter__()这个方法有什么用?

​     返回值返回该对象的一个迭代器

```python
dic = {'a':1,'b':2,'c':3}
res = dict.__iter__()
print(res)
       
输出:
<dict_keyiterator object at 0x0093AC08>
```

<!--more-->

## 4.转化成迭代器后怎么取值？

直接用__next__方法取值

```python
#续上
print(res.__next__()) #a
print(res.__next__()) #b
print(res.__next__()) #c
print(res.__next__()) #error:StopIteration

输出:
a
b
c
Traceback (most recent call last):
  File "C:/Users/Charles/PycharmProjects/练习/迭代器.py", line 67, in <module>
    print(res.__next__()) #error:StopIteration
StopIteration
```

## 5.通用的迭代器取值的方法

​     

```python
while True:
       try:
           print(obj_iter.__next__())
       except StopIteration:
           break
```


​      

```python
#input.txt
轻轻的我走了，
正如我轻轻的来；
我轻轻的招手，
作别西天的云彩。

#遍历迭代器的方法
with open('input',mode = 'r',encoding ='utf-8') as f:
     iter=f.__iter__()
     while True:
        try:
           print(iter.__next__(),end='')
        except  StopIteration:
           print('\n文件遍历完成')
           break
            
输出:
轻轻的我走了，
正如我轻轻的来；
我轻轻的招手，
作别西天的云彩。
文件遍历完成
```

##    6.由上面的步骤可见 ，迭代访问对象过于麻烦

python提供了for循环简化遍历迭代器的语法

**for循环的工作原理**

1. 
   迭代器对象调用__iter__返回的是迭代器,迭代器指向的位置不变
2. 调用obj。__next__()获得迭代器对象
3. obj.__next__()获得返回值 ，赋值给k
4. 循环往复 直到捕捉到异常StopIteration 跳出循环



##    7 可迭代对象不是迭代器对象，迭代器对象是可迭代对象

​       列表，字符串，元组，字典，集合 是可迭代对象，但不是迭代器对象(有__iter__方法，没有__next__方法)
​       打开的文件 是迭代器对象，也是可迭代对象

​    **数据类型转化 list(’Hello‘) 底层实现也是for 循环，所以list()的参数是可迭代对象**

## 8. 优点：

1、为序列和非序列类型提供了一种统一的迭代取值方式。

2、惰性计算：迭代器对象表示的是一个数据流，可以只在需要时才去调用**next**来计算出一个值，就迭代器本身来说，同一时刻在内存中只有一个值，因而可以存放无限大的数据流，而对于其他容器类型，如列表，需要把所有的元素都存放于内存中，受内存大小的限制，可以存放的值的个数是有限的。



## 9. 缺点：

1、除非取尽，否则无法获取迭代器的长度

2、只能取下一个值，不能回到开始，更像是‘一次性的’，迭代器产生后的唯一目标就是重复执行next方法直到值取尽，否则就会停留在某个位置，等待下一次调用next；若是要再次迭代同个对象，你只能重新调用iter方法去创建一个新的迭代器对象，如果有两个或者多个循环使用同一个迭代器，必然只会有一个循环能取到值。




















