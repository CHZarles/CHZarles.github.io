---
title: 变量/基本数据类型/输入输出
comments: true
copyright: false
date: 2020-07-28 09:42:43
tags:
categories: python基本语法
photo:
top:
cover: https://tva1.sinaimg.cn/large/87c01ec7gy1fsnqqi27uej21kw0w04cl.jpg
toc: true
---

# NOTE

```python
#0.1 变量
#ref:https://zhuanlan.zhihu.com/p/108679428
#id相当于'身份证' ,id指名了值的内存地址 id相同的就是同一个变量
#'is'有来判断id是否相同,'=='用来判断值是否相同
#type内置方法 显示类型
a = 10
b = a
print(type(a))
print(id(a),id(b),a is b)
```

```python
#1.1 整型 浮点型
#作用:用来描述数量，记录
#定义: 浮点型整形虽然不是同一类型，但这两种类型之间可以算术运算。一般不同类型之间不能混起来运算，数字型就不能和字符串型运算。
age = 18
temperature = 36.8
```

```python
#1.2 字符串
#作用:用来描述
#定义: 单引号，双引号，三引号都可以定义字符串，只要引号成对出现合理嵌套就行。反斜杠可转义。
#注: 字符串可以用 * + 运算符。 效率低 开发不推荐用
str0 = 'helloworld'
str1 = " 我说'helloworld' "
str2 = ' 你说"helloworld" '
str3 = ' 他说\'helloworld\''
str4 = '''
    三引号可以用来写注释，也可以定义字符串
    引号内任意换行都可以
      '''
```

```
#1.3列表类型
#作用:有序按位置存任意类型的多个值。可通过索引取数.正序从0开始，逆序从-1开始
#定义: 中括号'[]' 内用逗号',' 分开各类型值
stu_info =[
    ['张三',18,'中国矿业大学',{'计算机网络':95,'操作系统':100,'高数A1':99}],
    ['李四',18,'中国矿业大学',{'计算机网络':75,'操作系统':80,'高数A1':92}]
]
```

```python
#1.4字典类型
#作用：将一个不可变类型的量key映射到另一个类型,key通常为字符串，用来描述值的含义
#定义: 用花括号'{}',key和对应值间用':'  值对之间用','
dict = {'a':1,'b':2}
```

```python
#1.5元组类似
#作用:元组与列表类似，也是可以存多个任意类型的元素，不同之处在于元组的元素不能修改，即元组相当于不可变的列表，用于记录多个固定不允许修改的值，单纯用于取
#定义:在()内用逗号分隔开多个任意类型的值,如果元组内只有一个值，则必须加一个逗号，否则()就只是包含的意思而非定义元组

countries = ("中国"，"美国"，"英国")  # 本质:countries = tuple("中国"，"美国"，"英国")
countries = ("中国"，)  # 本质:countries = tuple("中国")
```

```python
#1.6集合
#作用:集合、list、tuple、dict一样都可以存放多个值，但是集合主要用于：去重、关系运算
"""
定义：在{}内用逗号分隔开多个元素，集合具备以下三个特点：
     1：每个元素必须是不可变类型
     2：集合内没有重复的元素
     3：集合内元素无序
"""
s = {1,2,3,4}  # 本质 s = set({1,2,3,4})

注意1：列表类型是索引对应值，字典是key对应值，均可以取得单个指定的值，而集合类型既没有索引也没有key与值对应，所以无法取得单个的值，而且对于集合来说，主要用于去重与关系元素，根本没有取出单个指定值这种需求。

注意2:{}既可以用于定义dict，也可以用于定义集合，但是字典内的元素必须是key:value的格式，现在我们想定义一个空字典和空集合，该如何准确去定义两者?

d = {} # 默认是空字典 
s = set() # 这才是定义空集合
```



```python
#1.7布尔类型
#作用:记录真和假
#定义: 注意首字母大写
flag0 = True
flag1 = False
```



```python
#2.1用户交互输入
在python3中input功能会等待用户的输入，用户输入任何内容，都存成字符串类型，然后赋值给等号左边的变量名
username=input('请输入您的用户名：')
了解知识：
1、在python2中存在一个raw_input功能与python3中的input功能一模一样
2、在python2中还存在一个input功能，需要用户输入一个明确的数据类型，输入什么类型就存成什么类型
l=input('输入什么类型就存成什么类型: ')
#输入什么类型就存成什么类型: [1,2,3]
```



```python
#2.2格式化输出
print('hello world')  # 输出:hello world
print('first','second','third')  # 一次性输出多个值，值用逗号隔开。输出: first second third
默认print功能有一个end参数，该参数的默认值为"\n"(代表换行)，可以将end参数的值改成任意其它字符
print("aaaa",end='')
print("bbbb",end='&')
print("cccc",end='@')
#整体输出结果为：aaaabbbb&cccc@
%s占位符：可以接收任意类型的值 %d占位符：只能接收数字
print('亲爱的%s你好！你%s月的话费是%d，余额是%d' %('tony',12,103,11))
#输出:亲爱的tony你好！你12月的话费是103，余额是11
练习1：接收用户输入，打印成指定格式
name = input('your name: ')
age = input('your age: ') #用户输入18,会存成字符串18,无法传给%d
print('My name is %s,my age is %s' %(name,age))
```





<!--more-->














