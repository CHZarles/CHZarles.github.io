---
title: 面向对象-继承
comments: true
copyright: false
toc: true
date: 2020-08-15 10:50:05
tags:
categories: python基本语法
photo:
top:
cover:
---



## 什么是继承

**I:继承是种创建新类的方式， 新建的类可称为子类或派生类，父类又可称为基类或超类**
**II:需要注意的是: python支持多继承，在Python中，新建的类可以继承一个或多个父类**

```python
class Parent1:
    pass
class Parent2:
    pass
class  Sub1(Parent1):#单继承
    pass
class Sub2(Parent1,Parent2): #多继承
    pass

#查看父类
print(Sub1.__bases__) #输出：(<class '__main__.Parent1'>,)

#在python2中有经典类与新式类之分
#新式类:继承了object类的子类，以及该子类的子类子子类。
#经典:没有继承object类的子类，以及该子类的子类子子类。

# 在Python3中，即使没有显式地继承object，也会默认继承该类
```

<!--more-->



## 为何要用继承？

继承可以获得父类的属性，用来解决类与类之间代码冗余问题

 **python的多继承**

- **优点:**
  - **子类可以同时遗传多个父类的属性，最大限度地重用代码**

- **缺点:**
  - **违背人的思维习惯:继承表达的是一种什么"是"什么的关系**
  - **代码可读性会变差**
  -  **不建议使用多继承，扩展性变差, 如果真的涉及到一个子类不可避免地要重用多个父类的属性，应该使用Mixins**





## 实现继承（单继承）

demo

```python
class Cumter:
    school = 'CUMT'

    def __init__(self,name,age,sex):
        self.name = name
        self.age = age
        self.sex = sex

class Student(Cumter):
    #__init__直接用父类的
    #def __init__(self,name,age,sex):
    #    self.name = name
    #    self.age = age
    #    self.sex = sex

    def choose_course(self):  #子类的全新的功能
         print('%s 正向选课' %self.name)


class Teacher(Cumter):

    def __init__(self,name,age,sex,salary,level):
        # 指名道姓地执行父类的__init__,记得self要显式地添加上去
        Cumter.__init__(self,name,age,sex)
        self.salary = salary
        self.level = level

    def score(self):
        print('老师%s 正在给学生打分' %self.name)


tec_obj = Teacher('LIli','28','female','2K','PHD')
print(tec_obj.__dict__)
```

## 单继承属性查找

有了继承关系，对象在查找属性时，先从对象自己的\__dict__中找，如果没有则去子类中找，然后再去父类中找……

父类如果不想让子类覆盖自己的方法，可以采用双下划线开头的方式将方法设置为私有的

**!!!!!!!!!!单继承背景下的属性查找!!!!!!!!!!!!!!!!!!!!!!**

```python
class Foo:
    def f1(self):
        print('Foo.f1 ' )
    def f2(self):
        print('Foo.f2')
        self.f1()


class Bar(Foo):
    def f1(self):
        print( 'Bar.f1')
obj = Bar()
obj.f2()
'''输出:
Foo.f2
Bar.f1  
'''
```

**mark 上面输出的第二行 输出的是 Bar.f1 并不是 Foo.f1**
**如果想要输出的是 Foo.fl 可以像下面那样改**

```python
class Foo:
    def f1(self):
        print('Foo.f1 ' )
    def f2(self):
        print('Foo.f2')
        Foo.f1(self)
```





## 菱形继承

demo

```python
class A(object):
    def test(self):
        print('from A')

class B(A):
    def test(self):
        print('from B')

class C(A):
    def test(self):
        print('from C')

class D(B,C):
    pass

obj = D()
obj.test() # 结果为：from B
```

**为什么是这个结果？从继承的原理看起**

python到底是如何实现继承的呢？ 对于你定义的每一个类，Python都会计算出一个方法解析顺序(MRO)列表，该MRO列表就是一个简单的所有基类的线性顺序列表（应该系统就是根据这个表识别是不是谁是谁的父类,即使不是真正逻辑意义上的父类和基类），如下

```python
print(D.mro())
'''输出:
[<class '__main__.D'>, <class '__main__.B'>, <class '__main__.C'>, <class '__main__.A'>, <class 'object'>]
'''
# python会在MRO列表上从左到右开始查找基类,直到找到第一个匹配这个属性的类为止
```

由对象发起的属性查找，会从对象自身的属性里检索，没有则会按照对象的类.mro()规定的顺序依次找下去，
由类发起的属性查找，会按照当前类.mro()规定的顺序依次找下去，



## 多继承的正确打开方式:Mixins机制(一种规范)

- Mixins机制核心: 
  - 就是在多继承背景下尽可能地提升多继承的可读性
  - Mixins 类应该是一种功能 不能是一种东西

```python
#初始版本
class Vehicle:  # 交通工具
    def fly(self):
        '''
        飞行功能相应的代码
        '''
        print("I am flying")


class CivilAircraft(Vehicle):  # 民航飞机
    pass


class Helicopter(Vehicle):  # 直升飞机
    pass


class Car(Vehicle):  # 汽车并不会飞，但按照上述继承关系，汽车也能飞了
    pass

# 改进版本
# 表达基本性 表达 XX is XX 的类 要放在后面
class Vehicle:  # 交通工具
    pass

class flyableMixin:
    def fly(self):
        pass


class CivilAircraft(flyableMixin,Vehicle):  # 民航飞机
    pass


class Helicopter(flyableMixin,Vehicle):  # 直升飞机
    pass


class Car(Vehicle):  # 汽车并不会飞，但按照上述继承关系，汽车也能飞了
    pass

```



## 派生与方法重用

子类可以派生出自己新的属性，在进行属性查找时，子类中的属性名会优先于父类被查找，例如每个老师还有职称这一属性，我们就需要在Teacher类中定义该类自己的__init__覆盖父类的
**方式一 指名道姓的用 不依赖继承**

```python
class Cumter:
    school = 'CUMT'

    def __init__(self,name,age,sex):
        self.name = name
        self.age = age
        self.sex = sex

class Student(Cumter):
    #__init__直接用父类的
    #def __init__(self,name,age,sex):
    #    self.name = name
    #    self.age = age
    #    self.sex = sex

    def choose_course(self):  #子类的全新的功能
         print('%s 正向选课' %self.name)


class Teacher(Cumter):

    def __init__(self,name,age,sex,salary,level):
        # 指名道姓地执行父类的__init__,记得self要显式地添加上去
        Cumter.__init__(self,name,age,sex)
        self.salary = salary
        self.level = level

    def score(self):
        print('老师%s 正在给学生打分' %self.name)



tec_obj = Teacher('LIli','28','female','2K','PHD')
print(tec_obj.__dict__)
```

**方法二：super()**
**调用super()会得到一个特殊的对象，该对象会 “参照发起属性查找的类” 的MRO，且严格按照MRO规定的顺序向后查找**

```python
class A:
    def test(seLf):
        print( 'from A')
        super(). test() #不需要额外传入self
class B:
    def test(self):
        print('from B')


class C(A,B):
    pass

obj = C()
obj.test() #这里按照class c 的mro 来找对应的函数 找到第一个匹配的
```

## 组合

在一个类中以另外一个类的对象作为数据属性，称为类的组合。组合与继承都是用来解决代码的重用性问题。不同的是：继承是一种“是”的关系，比如老师是人、学生是人，当类之间有很多相同的之处，应该使用继承；而组合则是一种“有”的关系，比如老师有生日，老师有多门课程，当类之间有显著不同，并且较小的类是较大的类所需要的组件时，应该使用组合，如下示例

```python
class Course:
    def __init__(self,name,period,price):
        self.name=name
        self.period=period
        self.price=price
    def tell_info(self):
        print('<%s %s %s>' %(self.name,self.period,self.price))

class Date:
    def __init__(self,year,mon,day):
        self.year=year
        self.mon=mon
        self.day=day
    def tell_birth(self):
       print('<%s-%s-%s>' %(self.year,self.mon,self.day))

class People:
    school='清华大学'
    def __init__(self,name,sex,age):
        self.name=name
        self.sex=sex
        self.age=age

#Teacher类基于继承来重用People的代码，基于组合来重用Date类和Course类的代码
class Teacher(People): #老师是人
    def __init__(self,name,sex,age,title,year,mon,day):
        super().__init__(name,age,sex)
        self.birth=Date(year,mon,day) #老师有生日
        self.courses=[] #老师有课程，可以在实例化后，往该列表中添加Course类的对象
    def teach(self):
        print('%s is teaching' %self.name)


python=Course('python','3mons',3000.0)
linux=Course('linux','5mons',5000.0)
teacher1=Teacher('lili','female',28,'博士生导师',1990,3,23)

# teacher1有两门课程
teacher1.courses.append(python)
teacher1.courses.append(linux)

# 重用Date类的功能
teacher1.birth.tell_birth()

# 重用Course类的功能
for obj in teacher1.courses:
    obj.tell_info()
```