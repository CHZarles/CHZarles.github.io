---
title: 离散化模板
comments: true
copyright: false
date: 2020-07-20 10:30:34
tags:
categories: 
	- 基础算法
	- 入门算法
photo:
top:
cover: http://ww1.sinaimg.cn/large/006eb5E0gy1ggx8klq15xj30ng0al77s.jpg
toc: true
---













## **作用**:**把一连串不连续的数映射成从0或1开始的自然数(数很大的时候，弥补了将数对应到数组下标的不足)**

<!--more-->

![image.png](https://gitee.com/chzarles/images/raw/master/imgs/006eb5E0gy1ggx8klq15xj30ng0al77s.jpg)



**模板**

```c++
vector<int> alls; // 存储所有待离散化的值
sort(alls.begin(), alls.end()); // 将所有值排序
alls.erase(unique(alls.begin(), alls.end()), alls.end());   // 去掉重复元素 , 都是库函数

// 二分求出x对应的离散化的值
int find(int x) // 找到第一个大于等于x的位置，就是lower_bound写法
{
    int l = 0, r = alls.size() - 1;
    while (l < r)
    {
        int mid = l + r >> 1;
        if (alls[mid] >= x) r = mid;
        else l = mid + 1;
    }
    return r + 1; // 映射到1, 2, ...n，如果映射到0,1,2,....n就返回r就行
}

作者：yxc
链接：https://www.acwing.com/blog/content/277/
来源：AcWing
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
```



**Unique函数的实现**

```c++
//用到了双指针算法
vector<int> ::iterator unique(&a[])
{
    int j = 0 ;
    for(int i = 0 ; i < a.size() ; i ++)
        if(!i||a[i--]!=a[i])
        {
            a[j++] = a[i];
        }
    return begin()+j;
}
```





**例题**：[区间和](https://www.acwing.com/problem/content/804/)